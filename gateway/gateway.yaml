# apiVersion: gateway.networking.k8s.io/v1
# kind: Gateway
# metadata:
#   name: gateway
#   namespace: default
# spec:
#   gatewayClassName: kong
#   listeners:
#   - name: http
#     port: 80
#     protocol: HTTP
#     # hostname: "*.127.0.0.1.nip.io"
#     allowedRoutes:
#       namespaces:
#         from: All
